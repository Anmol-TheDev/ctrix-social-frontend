// types.ts (optional: good for reusability)
export type User = {
    userName: string
    userEmail: string
    userAvatar: string
    isLoggedIn: boolean
  }
  
  export type UserProfile = {
    userBio: string
    followers: string[]
    following: string[]
    postCount: number
    bio: string
  }
  

